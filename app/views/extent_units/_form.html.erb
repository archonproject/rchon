<%= form_for(@extent_unit) do |f| %>
  <% if @extent_unit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@extent_unit.errors.count, "error") %> prohibited this extent_unit from being saved:</h2>

      <ul>
      <% @extent_unit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
